define([
	'jquery',
	'underscore',
	'backbone',
], function ($, _, Backbone) {
    var ListView = Backbone.View.extend({
        el: $('.main__display'),
        initialize: function() {
        },
        render: function() {
        }
    });
    return ListView;
});